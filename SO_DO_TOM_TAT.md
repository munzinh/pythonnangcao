# 📊 SƠ ĐỒ TÓM TẮT - TASKMASTER

## 1. KIẾN TRÚC TỔNG QUAN

```
┌─────────────────────────────────────────────────────────┐
│                    CLIENT (Browser)                     │
│  ┌──────────────────────────────────────────────────┐  │
│  │  HTML + Bootstrap 5 + JavaScript                  │  │
│  │  - Giao diện người dùng                          │  │
│  │  - AJAX requests                                 │  │
│  └──────────────────────────────────────────────────┘  │
└────────────────────┬────────────────────────────────────┘
                     │ HTTP/HTTPS (RESTful API)
                     ▼
┌─────────────────────────────────────────────────────────┐
│              FLASK APPLICATION SERVER                   │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Blueprints:                                      │  │
│  │  • Main Routes (/)                                │  │
│  │  • API Routes (/api)                                │  │
│  │  • Error Handlers                                 │  │
│  └──────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────┐  │
│  │  TaskManager (Business Logic)                    │  │
│  │  • create_task()                                │  │
│  │  • get_all_tasks()                              │  │
│  │  • update_task()                                │  │
│  │  • delete_task()                                │  │
│  │  • search_tasks()                               │  │
│  └──────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────┐  │
│  │  SQLAlchemy ORM                                  │  │
│  │  • Task Model                                    │  │
│  │  • Database Queries                             │  │
│  └──────────────────────────────────────────────────┘  │
└────────────────────┬────────────────────────────────────┘
                     │ SQL Queries
                     ▼
┌─────────────────────────────────────────────────────────┐
│                  MYSQL DATABASE                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │  Table: tasks                                    │  │
│  │  • id (PK)                                       │  │
│  │  • title, description                            │  │
│  │  • completed, priority                           │  │
│  │  • created_at, updated_at                        │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

## 2. LUỒNG TẠO TASK (POST /api/tasks)

```
[Browser]
  │ User điền form → Click "Add Task"
  │
  ▼
[JavaScript]
  │ Collect data: {title, description, priority}
  │ fetch('POST /api/tasks', {body: JSON.stringify(data)})
  │
  ▼
[Flask API Route]
  │ @bp.route('/tasks', methods=['POST'])
  │ Parse JSON: request.get_json()
  │ Validate: title required
  │
  ▼
[TaskManager.create_task()]
  │ Validate input
  │ Create Task object
  │ db.session.add(task)
  │ db.session.commit()
  │
  ▼
[SQLAlchemy]
  │ Generate SQL: INSERT INTO tasks ...
  │
  ▼
[MySQL Database]
  │ Execute INSERT
  │ Return: new task ID
  │
  ▼
[TaskManager]
  │ task.to_dict() → Convert to dict
  │ Return: {'id': 5, 'title': '...', ...}
  │
  ▼
[API Response]
  │ HTTP 201 Created
  │ JSON: {"success": true, "data": {...}, "message": "..."}
  │
  ▼
[Browser]
  │ Parse response
  │ Refresh task list
  │ Show success notification
```

## 3. LUỒNG LẤY DANH SÁCH TASKS (GET /api/tasks)

```
[Browser]
  │ Page load / User click "Refresh"
  │
  ▼
[JavaScript]
  │ fetch('GET /api/tasks')
  │
  ▼
[Flask API Route]
  │ @bp.route('/tasks', methods=['GET'])
  │ Get query param: completed (optional)
  │
  ▼
[TaskManager.get_all_tasks()]
  │ Build query: Task.query
  │ Filter by completed (if provided)
  │ Order by created_at DESC
  │ Execute: .all()
  │
  ▼
[SQLAlchemy]
  │ Generate SQL: SELECT * FROM tasks ORDER BY created_at DESC
  │
  ▼
[MySQL Database]
  │ Execute query
  │ Return: rows
  │
  ▼
[TaskManager]
  │ Convert: [task.to_dict() for task in tasks]
  │ Return: [{...}, {...}, ...]
  │
  ▼
[API Response]
  │ HTTP 200 OK
  │ JSON: {"success": true, "data": [...], "count": 5}
  │
  ▼
[Browser]
  │ Parse JSON
  │ Render tasks to HTML table
```

## 4. CẤU TRÚC THƯ MỤC

```
TaskMaster/
├── app/                    # Main package
│   ├── __init__.py        # Application Factory
│   ├── config.py          # Configuration
│   ├── models.py          # Task Model + TaskManager
│   │
│   ├── api/               # API Blueprint
│   │   └── routes.py      # API endpoints
│   │
│   ├── main/              # Web UI Blueprint
│   │   └── routes.py      # HTML routes
│   │
│   ├── static/            # Static files
│   │   ├── css/main.css
│   │   └── js/main.js
│   │
│   └── templates/         # HTML templates
│       └── index.html
│
├── migrations/             # Database migrations
├── Dockerfile
├── docker-compose.yml
└── requirements.txt
```

## 5. DATABASE SCHEMA

```
┌─────────────────────┐
│       TASKS         │
├─────────────────────┤
│ id (PK, AI)         │
│ title (VARCHAR 200) │
│ description (TEXT)  │
│ completed (BOOL)    │
│ priority (VARCHAR)  │
│ created_at (DATETIME)│
│ updated_at (DATETIME)│
└─────────────────────┘
```

## 6. API ENDPOINTS

```
GET    /api/tasks              → Lấy danh sách tasks
GET    /api/tasks/<id>         → Lấy 1 task
POST   /api/tasks              → Tạo task mới
PUT    /api/tasks/<id>         → Cập nhật task
DELETE /api/tasks/<id>         → Xóa task
GET    /api/tasks/search?q=... → Tìm kiếm
GET    /api/tasks/export/csv   → Export CSV
GET    /api/tasks/export/json  → Export JSON
```

## 7. DESIGN PATTERNS

```
┌─────────────────────────────────────┐
│  1. Application Factory Pattern     │
│     - create_app() trong __init__.py│
│     - Linh hoạt, dễ test            │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  2. Blueprint Pattern               │
│     - Tách routes thành modules     │
│     - Main, API, Errors blueprints  │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  3. ORM Pattern (SQLAlchemy)        │
│     - Abstraction cho database      │
│     - Không cần viết SQL thủ công   │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  4. Repository Pattern (TaskManager)│
│     - Tách business logic           │
│     - Dễ test và maintain           │
└─────────────────────────────────────┘
```

## 8. DOCKER ARCHITECTURE

```
┌─────────────────────────────────────┐
│     Docker Compose                  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │  MySQL Container              │  │
│  │  - Port: 3307                 │  │
│  │  - Database: taskmaster_db    │  │
│  │  - Init: init.sql             │  │
│  └───────────────────────────────┘  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │  Flask Container              │  │
│  │  - Port: 5001                 │  │
│  │  - Migrations: auto run       │  │
│  │  - App: python run.py         │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

## 9. RESPONSE FORMAT

```
Success Response:
{
  "success": true,
  "data": {...} hoặc [...],
  "message": "..." (optional),
  "count": 5 (cho list endpoints)
}

Error Response:
{
  "success": false,
  "error": "Error message here"
}
```

## 10. TÍNH NĂNG CHÍNH

```
┌─────────────────────────────────────┐
│  ✅ CRUD đầy đủ                     │
│     - Create, Read, Update, Delete  │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  🔍 Tìm kiếm                        │
│     - Search by title/description   │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  🎯 Lọc dữ liệu                     │
│     - Filter by completed status    │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  📊 Xuất dữ liệu                    │
│     - Export CSV và JSON            │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  🎨 Giao diện                       │
│     - Responsive với Bootstrap 5    │
└─────────────────────────────────────┘
```

---

*Các sơ đồ này có thể được sử dụng trong PowerPoint hoặc bản trình bày.*

